apiVersion: v1
kind: Service
metadata:
  name: {{ include "wf-springboot.fullname" . }}
  labels:
    {{- include "wf-springboot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wf-springboot.selectorLabels" . | nindent 4 }}
