apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wf-springboot.fullname" . }}-test-connection"
  labels:
    {{- include "wf-springboot.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wf-springboot.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
